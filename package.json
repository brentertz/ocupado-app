{
  "name": "ocupado-atom-build",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "npm list -g grunt-cli || npm install -g grunt-cli",
    "start": "grunt start",
    "debug": "grunt debug"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-download-atom-shell": "^0.10.0",
    "grunt-shell": "^1.1.1"
  }
}
